<footer class="site-footer fixed-bottom hidden">
    <div class="footer-wrapper py-3">
        <div class="container">
            <div class="row g-3 align-items-center justify-content-center text-center">
                <div class="col-auto">
                    <a href="/aboutus" class="footer-link">About us</a>
                </div>
                <div class="col-auto">
                    <a href="/contactUs" class="footer-link">Contact Us</a>
                </div>
                <div class="col-auto">
                    <a href="/Help" class="footer-link">Customer Support</a>
                </div>
                <div class="col-auto">
                    <a href="https://www.flaticon.com/free-icons/default" class="footer-link">User Icon</a>
                </div>
                <div class="col-auto">
                    <span class="footer-text">&copy; YelpCamp 2025</span>
                </div>
            </div>
        </div>
    </div>

    <style>
        .site-footer {
            transform: translateY(100%);
            transition: transform 0.3s ease-in-out;
            z-index: 1030;
        }

        .site-footer.visible {
            transform: translateY(0);
        }

        .footer-wrapper {
            background: rgba(24, 24, 24, 0.8);
            backdrop-filter: blur(8px);
            border-top: 1px solid rgba(0, 0, 0, 0.1);
        }

        .footer-link {
            color: var(--muted);
            text-decoration: none;
            transition: color 0.2s ease;
            padding: 0.5rem 1rem;
            font-size: 0.9rem;
        }

        .footer-link:hover {
            color: var(--accent);
        }

        .footer-text {
            color: var(--muted);
            font-size: 0.9rem;
        }

        @supports not (backdrop-filter: blur(8px)) {
            .footer-wrapper {
                background: rgba(15, 14, 14, 0.95);
            }
        }
    </style>

    <!-- footer scroll handler moved to a separate client JS file -->
    <script src="/javascripts/footScrollHandler.js"></script>
</footer>